import _ from 'lodash'

const initialState = {
  //Add initial state
}

const home = (state = initialState, action) => {
  switch (action.type) {
    //Add cases based on action type
    default:
      return state
  }
}

export default home