import _ from 'lodash'

const initialState = {
  //Add initial state
}

const join = (state = initialState, action) => {
  switch (action.type) {
    //Add cases based on action type
    default:
      return state
  }
}

export default join